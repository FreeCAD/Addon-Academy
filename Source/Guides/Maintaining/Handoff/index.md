---
layout : Default
---

# Maintainer handoff

*Work in progress.*

**Planned scope:**

-   Marking an addon as orphaned (the `no-one@freecad.org` maintainer convention).
-   Transferring the repository to a new maintainer.
-   Updating the manifest `<maintainer>` and `<url>` entries.
-   Communicating the change to users via the changelog and overview page.
