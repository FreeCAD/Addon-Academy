---
layout : Default
---

# Custom document objects

*Work in progress.*

**Planned scope:**

-   `FeaturePython` vs scripted objects vs `DocumentObjectGroupPython`.
-   Adding properties with `addProperty()` and choosing property types.
-   The `execute()` lifecycle and dependency recomputation.
-   ViewProviders: attaching, claiming children, custom display modes.
-   Serialization, `__getstate__` / `__setstate__`, and schema migrations.
