---
layout : Default
---

# Gui Commands

*Work in progress.*

**Planned scope:**

-   Subclassing `FreeCADGui.Command` and its methods (`Activated`, `IsActive`, `GetResources`).
-   Registering commands with `FreeCADGui.addCommand()`.
-   Placing commands in toolbars and menus via `Workbench.appendToolbar()` / `appendMenu()`.
-   Command icons, tooltips, and keyboard shortcuts.
-   Visibility and availability rules (`IsActive`).
