---
layout : Default
---

# Workbench registration

*Work in progress.*

**Planned scope:**

-   Structure of `init_gui.py` and the `Workbench` class.
-   The `Initialize`, `Activated`, `Deactivated`, and `ContextMenu` hooks.
-   Registering with `Gui.addWorkbench()`.
-   The `classname` manifest entry and how FreeCAD locates your class.
-   Keeping startup work minimal so FreeCAD launches quickly.
