---
layout : Default
---

# Debugging

*Work in progress.*

**Planned scope:**

-   Using the FreeCAD Python console interactively.
-   Attaching an IDE debugger (VS Code, PyCharm) to a running FreeCAD.
-   Reading tracebacks from the Report view.
-   Interactive debuggers in GUI/non-GUI contexts (`breakpoint()`, `pdb`, `ipdb`).
-   Manifest validation via the Addon Manager's `developerMode`.
